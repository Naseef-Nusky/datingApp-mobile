# Local browser dev (npm run dev) — use Vite proxy, avoids CORS errors
# Leave VITE_API_URL empty so requests go to http://localhost:3001/api/mobile/* → proxied to backend
VITE_API_URL=

# Match your backend PORT (see datingApp-backend/.env, default 5001)
VITE_DEV_PROXY_API=http://localhost:5001
